// You will need to replace the API key placeholder in the: appsettings.json file, with your actual API key to run the demo successfully

// If you want to use the free version of the API to test, you will also need to change the URL being called, which can be found where the API key is also being used. 

// See the example C# code in the API test page on RapidAPI.com
